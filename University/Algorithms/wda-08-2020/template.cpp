#include <iostream>

using namespace std;

int main (){

    ios_base::sync_with_stdio(0);
    int volume;

    bool wyklad = true;


    while(true){
        volume = 100;
        while(volume-- > 0);
    }

    return 0;
}